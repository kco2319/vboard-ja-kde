# CHANGES

本家 [mdev588/vboard](https://github.com/mdev588/vboard) からの変更点まとめです。

## JIS配列対応

### キーコードの再割当

US配列とJIS配列で刻印が異なるキーを、JIS (pc106) の刻印に合わせてラベル・シフト記号を再定義しました。

| キーイベント | ラベル | Shift時 |
|---|---|---|
| `KEY_LEFTBRACE` | `@` | `` ` `` |
| `KEY_RIGHTBRACE` | `[` | `{` |
| `KEY_BACKSLASH` | `]` | `}` |
| `KEY_APOSTROPHE` | `:` | `*` |
| `KEY_EQUAL` | `^` | `~` |
| `KEY_MINUS` | `-` | `=` |
| `KEY_SEMICOLON` | `;` | `+` |

数字キーのShift記号もJIS準拠に変更しています（`2→"`、`6→&`、`7→'`、`8→(`、`9→)` 等）。`update_label()` 内の `button_positions` はこの再割当に合わせてすべて再計算しています。

### ¥・ろ(\\)キーの追加

最上段（Esc / F1〜F12 と同じ行）に、¥キー（`KEY_YEN`、Shift: `|`）とろキー（`KEY_RO`、Shift: `_`）を独立したボタンとして追加しました。

**既知バグと修正**：`KEY_RO` は python-uinput に定義がない環境があり、フォールバック値として誤って `uinput.KEY_BACKSLASH` を割り当てていたため、既存の `]` ボタンと入力が衝突し「¥と\が同じキーとして送信される」不具合がありました。フォールバックを独立したraw tuple `("KEY_RO", 89)` に変更して解決しています。

なお、一時的に¥と\を1ボタンに統合する変更を試しましたが、その後「Shift+\で`|`が出ない」問題が発覚したため、最終的に2ボタン構成に戻して確定しています。

### `(バッククォート)キーについて

`Shift+@`キーを強制送信してバッククォートを直接入力可能にする実装を検討しましたが、本環境では`(`KEY_GRAVE`)をMozcの半角/全角切替信号として使用しているため不採用とし、`KEY_GRAVE`をそのまま送信する元の実装を維持しています。

## UI改修（ヘッダーバー）

### 閉じる・最大化ボタンの長押し方式化

✕（閉じる）・□（最大化）ボタンをタップ即実行ではなく、600msの長押しで発火する方式に変更しました（`create_longpress_button`）。タッチ操作のみの環境で誤タップによる意図しないウィンドウ操作を防ぐための変更です。ネイティブの装飾ボタンからは外し、Gtk.Buttonによる自前実装に切り替えています。

### リサイズ方式の変更

ウィンドウリサイズについて、以下の順で検討・実装しました。

1. **KWinの`begin_resize_drag`（対話的リサイズ）** — plasmashellがクラッシュする事象を確認したため不採用（タッチ起点のリサイズグラブが原因と推測）。
2. **座標差分による手動`resize()`（ドラッグ方式）** — 1回目は成功するが2回目以降は反応しない不具合が発生したため不採用（タッチイベントとポインタイベントの二重グラブ、Gtk.Buttonの内部クリックジェスチャーがタッチシーケンスを奪う問題を推測）。
3. **「⤢+ / ⤢-」ステップボタン方式（最終採用）** — タップごとに40pxずつ幅・高さを増減する方式。ドラッグ検知に一切依存せず、既存の不透明度+/-ボタンと同じ実装パターンのため動作の確実性を確認済みです。

## その他

- 三本線メニューによるUI要素の表示/非表示切替を追加
- 背景色プリセット・不透明度の調整機能は本家から継承

## 未解決・保留事項

- plasmashellの古いコアダンプ（リサイズ問題とは無関係と判断）の根本的なクリーンアップは未着手です
